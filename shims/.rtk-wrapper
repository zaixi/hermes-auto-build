#!/bin/bash
# RTK wrapper: strip shims from PATH, delegate to RTK
# All shims in this directory delegate to this wrapper via exec.
PATH=$(echo ":$PATH:" | sed 's#:/usr/local/shims/*:#:#g; s#^:##; s#:$##')
export PATH
exec /usr/local/bin/rtk "$(basename "$0")" "$@"
