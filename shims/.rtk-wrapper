#!/bin/bash
# RTK wrapper: first arg is the real command name, resolve to absolute path
CMD="$1"
shift
PATH=$(echo ":$PATH:" | sed 's#:/usr/local/shims/*:#:#g; s#^:##; s#:$##')
export PATH
REAL_CMD=$(command -v "$CMD")
exec /usr/bin/rtk "$REAL_CMD" "$@"
